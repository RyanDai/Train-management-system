package railway;

/**
 * An enumerated type for representing the three different types of branches
 * that can connect junctions to sections on a railway track.
 *
 */
public enum Branch {
    FACING, NORMAL, REVERSE
}
